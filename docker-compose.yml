version: "2"

services:
  network-engine:
    image: gitlab.onlineterroir.com:5001/pitzzae/network-engine
#    image: network-remote
    container_name: network-engine
    restart: always
    network_mode: "bridge"